# 贡献指南

感谢参与改进。提交变更时请遵循以下约定：

1. 一个提交只处理一类问题，提交信息简洁说明影响范围。
2. RTL 变更需说明时钟域、流水线延迟、资源或时序影响。
3. 上位机变更需保持串口四字节协议与 UDP RGB565 协议兼容，或同步更新协议文档。
4. 不提交综合数据库、布局布线产物、可执行文件、录像或包含个人路径的配置。
5. 不删除或改写第三方文件中的版权和许可声明。

提交前运行：

```bash
python -m py_compile host/main.py
git grep -nE '[A-Za-z]:/' -- '*.qsf'
```

第二条命令正常情况下不应输出任何内容。

